# Steganography crypto configuration
# Copy to .env and set your values. Never commit .env to version control.

# AES-128 key (exactly 16 bytes UTF-8) for password encryption in JPEG metadata
STEGO_AES_KEY=your-16-byte-key

# AES-128 Initialization Vector (exactly 16 bytes UTF-8) for password encryption
STEGO_AES_IV=your-16-byte-iv

# Caesar cipher key for message obfuscation (integer 0–255)
STEGO_CAESAR_KEY=2
