# Production profile: optimized for deployment (Render, Docker)

# Thymeleaf: enable template cache (avoids re-parsing on every request)
spring.thymeleaf.cache=true

# Lazy initialization: faster startup, beans created on first use
spring.main.lazy-initialization=true
